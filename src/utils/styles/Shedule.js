import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  line: {
    width: '150%',
    height: 0.5,
    position: 'relative',
    left: '-10%',
  },
});

export default styles;
